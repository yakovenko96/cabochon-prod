<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_cabochon_activity_assignee_only" model="ir.rule">
        <field name="name">Cabochon users see only assigned activities</field>
        <field name="model_id" ref="mail.model_mail_activity"/>
        <field name="groups" eval="[(4, ref('cabochon_base.group_cabochon_user'))]"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
    </record>
</odoo>
